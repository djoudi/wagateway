interface:
  display_name: "Wagateway"
  short_description: "Repo-specific patterns and workflows for wagateway"
  default_prompt: "Use the wagateway repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true